# The Rust-Embedded Shading Language

WIP

Test-build command template:

```shell
cargo build --bin=reslc --release && RUSTC_WRAPPER=target/release/reslc cargo build --package=test-example-hello-world
```
